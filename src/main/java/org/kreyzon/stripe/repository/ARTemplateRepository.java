package org.kreyzon.stripe.repository;

import org.kreyzon.stripe.entity.ARTemplate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ARTemplateRepository extends JpaRepository<ARTemplate, Long> {
}